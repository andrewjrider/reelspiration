# Public canonical origin used by metadata and generated sitemap URLs.
NEXT_PUBLIC_SITE_URL=https://reelspiration.com

# Server-side HTTPS endpoint that accepts JSON: { "email": "...", "source": "reelspiration.com" }
NEWSLETTER_SIGNUP_URL=

# Optional bearer token for the configured signup endpoint.
NEWSLETTER_API_KEY=
